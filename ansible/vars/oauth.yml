# Google OIDC (OAUTH2) single sign on configuration
oauth_secrets: "~/secrets/oauth_secrets.yml" 
#oauth_client_id: ''      # set this in the oauth_secrets file
#oauth_client_secret: ''  # set this in the oauth_secrets file
oauth_callback_url: "https://{{ host }}:{{ jupyterhub_port }}/hub/oauth_callback"
